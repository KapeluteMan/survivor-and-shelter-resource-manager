#ifndef SURVIVOR_FILE_OPERATIONS_H
#define SURVIVOR_FILE_OPERATIONS_H

struct Survivor* add_survivors_from_file();

void add_all_to_file(struct Survivor* head);

void add_one_to_file(struct Survivor *to_add);

#endif //SURVIVOR_FILE_OPERATIONS_H